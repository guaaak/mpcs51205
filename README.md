"# mpcs51205" 

instructions:
```bash
cd user_management
docker-compose build
docker-compose up -d
cd ../buying
docker-compose build
docker-compose up -d
```

command for rebuild:
```bash
docker-compose up -d --build
```

